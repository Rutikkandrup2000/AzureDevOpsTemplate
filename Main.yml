# parameters:
# - name: Build
#   type: string
steps:
  - script: echo I did a thing
    
    condition:  eq('${{ variables.Build }}', 'Java')
    # condition: eq(variables['Build'], 'Java')
  - template: JavaTemplate.yml




