parameters:
  - name: Build
    default: Java
    type: string
steps:
- ${{ if eq(parameters.Build, 'Java') }}:
  - template: MavenBuild.yml
- ${{ else }}:
  - template: SonarCloud.yml
  - template: Docker.yml