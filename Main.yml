# parameters:
# - name: Build
#   type: string
steps:
  - script: echo "${{ variables.Build }}"
    condition:  eq('${{ variables.Build }}', 'Java')
    # condition: eq(variables['Build'], 'Java')
  - template: JavaTemplate.yml




