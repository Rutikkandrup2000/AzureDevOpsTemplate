parameters:
  - name: Build
    type: string
steps:
- ${{ and eq(parameters.Build, 'Java'), eq(variables.sonarQubeRunAnalysis, 'true') }}:
  - task: SonarCloudPrepare@1
  - template: MavenBuild.yml
- ${{ else }}:
  - template: SonarCloud.yml
  - template: Docker.yml